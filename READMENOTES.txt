

1. Start the Selenium Grid by going to the directory of your docker-compose.yaml file

2. Open cmd. Then type "docker-compose up"

3. Verify that Selenium Grid, as well as the hubs are running.
Go to "http://localhost:4444/grid/console". If you see the Grid Console screen, then it means the Grid is running.

4. Now you can start running the tests.